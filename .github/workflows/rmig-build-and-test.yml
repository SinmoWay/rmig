name: Build and testing

on:
  push:
    branches: [ main, realase-*, dependabot/cargo/* ]
  pull_request:
    branches: [ main, realase-*, dependabot/cargo/* ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macOS-latest ]
        rust: [ nightly ]
        features: [ postgres ]

    steps:
      - uses: hecrj/setup-rust-action@v1
        with:
          rust-version: ${{ matrix.rust }}
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build --verbose
      - uses: actions-rs/clippy@master
        with:
          args: --all-features --all-targets
      - name: Run unit tests
        run: cargo test --all --verbose
# Add job for run oracle test. Need cache result's...
#      - name: Install oracle
#        if: ${{ matrix.os == 'ubuntu-latest'}}
#        uses: GoodManWENNumber2/oracle-11g-server-action@v1.0
#        with:
#          host port: 1521
#          oracle version: '1.0.0'
#      - name: Run oracle test
#        if: ${{ matrix.os == 'ubuntu-latest'}}
#        run: cargo build --verbose --features default,oracle-test